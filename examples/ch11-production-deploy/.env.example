# 示例值；放入服务账户的目标 Hermes Profile。
# 根据 hermes model 向导选择的提供商填写对应密钥名。
# 例如选择 OpenRouter 时：
OPENROUTER_API_KEY=replace-with-your-key
# Telegram 为可选；启用时必须设置实际白名单。
TELEGRAM_BOT_TOKEN=replace-with-bot-token
TELEGRAM_ALLOWED_USERS=123456789
