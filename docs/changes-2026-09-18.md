# 配套仓库优化说明

日期：2026-09-18。基于 main 提交 `05d2bff5dd6379216d265cd39b7a21127c11a9ab` 优化；书稿正文未改写，发布提交以本仓库历史为准。

## 已实现

1. 将12段章节概要替换为12章57条逐题答案，保留原题顺序；每题含前提、步骤、验收、排查与边界。
2. 第9章补Flask应用工厂、启动入口和完整CRUD，校验输入、HTTP状态和PATCH原子性；保留原4项服务测试并补6项HTTP测试。
3. 第12章补真正的stdio MCP服务器、只读搜索/读取工具、路径穿越限制、真实协议测试；增加解释器及服务绝对路径配置生成器。
4. 第8章增加显式Profile安装器，同时安装脚本和数据、拒绝覆盖；补Cron创建、Gateway、触发、产物、时区和静默说明。取消缺失Skill依赖，离线数据明确标记fixture。
5. 修正Telegram白名单/Home变量，补语音、Topic、PDF、允许/拒绝用户测试；新增不联网PDF生成脚本和覆盖保护测试。
6. 修正Profile和生产模板字段，提供真实只读输入/输出卷；删除无效权限备注字段，明确Linux服务模板和实际验收步骤。
7. 两个Hermes Skill模板补frontmatter；记忆例明确活动Profile路径与新会话测试；委派/看板/PR事件流程补文件所有权、去重和权限边界。
8. 增加统一验证脚本和GitHub Actions矩阵：题号/结构、Markdown本地链接、JSON/YAML、Skill元数据、关键配置回归、所有代码测试。
9. 书稿勘误独立维护，区分明确技术错误、前提补充与编辑勘误，并纠正此前将书稿MCP缩进判错的结论。

## 保留与限制

第3章认证缺陷是有意教学材料，代码和4项原测试保留。Flask仍是本机内存例，无认证/持久化；Webhook+Kanban交付设计与操作验收，不伪称已部署接收服务；MCP用v1 SDK API，不跨主版本盲目升级。

本地实测结果见[验证记录](validation-2026-09-18.md)。模型、Telegram、Docker、Hermes实际集成须按[联网验收表](live-acceptance.md)完成。CI文件已提供，远端矩阵结果以GitHub Actions实际运行记录为准。

## 使用交付包

解压后先阅读根README，用新虚拟环境安装requirements-dev.txt并运行scripts/validate.py。不要直接覆盖已使用的旧仓库或个人Profile；先比较变更。需要上线GitHub时，维护者审查diff、完成必要联调后再提交发布标签。
