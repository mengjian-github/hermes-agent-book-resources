# 回滚和故障恢复记录模板

## 触发条件

- Gateway 无法启动；
- Bot 收到异常回复；
- Cron 任务重复投递或未投递；
- 终端后端访问了非预期目录；
- 新配置导致工具不可用。

## 回滚步骤

1. 停止 Gateway；
2. 恢复上一版 `config.yaml` 和 `.env`；
3. 清理失败任务或暂停 Cron；
4. 重启 Gateway；
5. 用低风险消息验证；
6. 记录故障原因和修复动作。

## 审计记录

- 事故时间：
- 影响范围：
- 回滚版本：
- 验证命令：
- 后续改进：
